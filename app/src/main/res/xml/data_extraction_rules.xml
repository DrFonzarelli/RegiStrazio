<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12 e successivi. Vedi backup_rules.xml per il perché di tutti
    questi `exclude`: i due file devono dire la stessa cosa, o l'app si
    comporta in un modo su un telefono e in un altro su quello accanto.

    Le due sezioni sono distinte apposta: `cloud-backup` è il backup su
    Google, `device-transfer` è la copia diretta quando si passa a un telefono
    nuovo. Escludere solo la prima lascerebbe passare dalla seconda gli stessi
    dati — compresa la `EncryptedSharedPreferences`, che senza la sua chiave
    del Keystore arriverebbe illeggibile.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
